import {isLogged} from "@/events/getters";
import { Router } from 'next/router'

const validateView = (View) => {
    return View
    // if(isLogged()){
    //
    // }
    // Router.push('/home')

}


export default {
    validateView
}